package co.gamep.sliced.services.std.display.logicalspace.paths;

class SegmentedPathBase implements IPath {
	public var numSegments(default,never) : Int;
	public var pointData(never,default) : Dynamic;
	public var segments(default,never) : Dynamic;
	public function new():Void {super();}}
